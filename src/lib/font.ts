import { Bebas_Neue } from 'next/font/google';
import { Poppins } from 'next/font/google';


export const headings = Bebas_Neue({
  weight: '400',
  subsets: ['latin'],
  display: 'swap',
});



export const secText = Poppins({
  weight: '400',
  subsets: ['latin'],
  display: 'swap',
});


export const secTextBold = Poppins({
  weight: '600',
  subsets: ['latin'],
  display: 'swap',
});